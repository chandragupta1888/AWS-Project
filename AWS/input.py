import numpy as np
import sys, json


# data = np.array(json.loads(sys.argv[1]))
data = sys.argv
print(data)
# std = np.std(data)
# avg = np.mean(data)
# min = np.min(data)
# max = np.max(data)
# result = {'std':std, 'avg':avg, 'min':min, 'max':max}
#print(json.dumps(result))